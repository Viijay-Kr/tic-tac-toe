overwrite: true
schema: "http://localhost:8000/graphql/"
documents: "pages/**/*.graphql"
generates:
  pages/__generated__/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
    config:
      skipTypename: true
      inlineFragmentTypes: combine
